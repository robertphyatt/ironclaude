{
  "name": "reopen_for_edit leftover-candidate discriminator (I-1)",
  "goal": "Fix I-1 — the ancestry reopen guard must refuse only a genuine this-lifecycle landed CAS (candidate descends from the lock's expected_target), letting a stale prior-lifecycle leftover candidate whose target has advanced proceed to a cleaning reopen. Folds into unpushed v1.1.11 alongside lineage 119.",
  "requirements_file": "docs/plans/2026-09-21-reopen-leftover-candidate-requirements.md",
  "design_file": "docs/plans/2026-09-21-reopen-leftover-candidate-design.md",
  "tasks": [
    {
      "id": 1,
      "name": "C1 — expected_target discriminator + test (integration.ts + tests)",
      "description": "reopen guard must refuse only a genuine landed CAS (candidate landed AND lock held AND isAncestor(lock.expected_target, candidate) = candidate descends from expected_target). A stale leftover candidate whose target advanced (ancestor of expected_target) proceeds to a cleaning reopen. Rewrite the guard comment (accurate, names the T==C_old known gap without overclaiming). TDD. Known gap (backlog, out of scope): T==C_old leftover still refused — pre-existing under lineage 118.",
      "allowed_files": ["worker/mcp-servers/workspace-manager/src/integration.ts", "worker/mcp-servers/workspace-manager/src/__tests__/integration-cases.ts"],
      "depends_on": [],
      "steps": [
        {"description": "RED: in integration-cases.ts recovery part add a stale-leftover test. setup() stages ONE file consumed by the first worktree commit, so the SECOND worktree commit MUST be --allow-empty (a plain commit with clean index exits 1 and the execFileSync git() helper throws). Seed: const {root,database,assignment}=setup(false); git(assignment.worktree_path,'commit','-m','prior lifecycle work'); const cOld=git(assignment.worktree_path,'rev-parse','HEAD'); git(root,'merge','--ff-only',cOld); git(root,'update-ref',`refs/ironclaude/finalization/${assignment.workspace_guid}/candidate`,cOld); git(root,'commit','--allow-empty','-m','target advanced past C_old'); const advancedTarget=git(root,'rev-parse','HEAD'); git(assignment.worktree_path,'commit','--allow-empty','-m','current lifecycle reviewed work'); const frozenF=git(assignment.worktree_path,'rev-parse','HEAD'); git(root,'update-ref',`refs/ironclaude/finalization/${assignment.workspace_guid}/frozen`,frozenF); acquireIntegrationLock(database,{repositoryIdentity:assignment.repository_identity,workspaceGuid:assignment.workspace_guid,targetRef:'refs/heads/main',expectedTarget:advancedTarget}); database.prepare(\"UPDATE assignments SET lifecycle_status='ready_for_integration' WHERE workspace_guid = ?\").run(assignment.workspace_guid); const result=reconcileFinalization(database,{repositoryPath:root,workspaceGuid:assignment.workspace_guid,providerRootSessionId:OWNER,rebaseRecovery:'reopen_for_edit'}); expect(result.state).toBe('finalization-reopened-for-edit'); expect(() => git(root,'rev-parse','--verify',`refs/ironclaude/finalization/${assignment.workspace_guid}/candidate`)).toThrow(); expect(database.prepare('SELECT 1 FROM integration_locks WHERE repository_identity = ? AND workspace_guid = ?').get(assignment.repository_identity, assignment.workspace_guid)).toBeUndefined().", "command": "cd /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/workspace-manager && npx vitest run src/__tests__/integration-recovery.test.ts", "expected": "RED: the leftover case fails because the current lineage-119 guard (landed && lockHeld) REFUSES it (throws 'reopen_for_edit refused' instead of proceeding). The lineage-119 genuine landed-then-advanced reopen-refuses case still passes."},
        {"description": "GREEN: in reopenForEdit's interrupted-CAS guard (~:1961-1977) replace the `SELECT 1` lockHeld check + `if (landed && lockHeld)` refusal with the expected_target discriminator and the softened comment: `let landedCandidate: string | undefined; try { landedCandidate = runGit(exact.primaryCheckoutPath, ['rev-parse','--verify',`${candidateRef(assignment.workspace_guid)}^{commit}`]).trim(); } catch {} if (landedCandidate) { const landed = isAncestor(exact.primaryCheckoutPath, landedCandidate, targetRef(assignment)); const lockRow = db.prepare('SELECT expected_target FROM integration_locks WHERE repository_identity = ? AND workspace_guid = ?').get(assignment.repository_identity, assignment.workspace_guid) as { expected_target: string } | undefined; if (landed && lockRow && isAncestor(exact.primaryCheckoutPath, lockRow.expected_target, landedCandidate)) { throw new Error('reopen_for_edit refused: integration already landed on the target (interrupted-CAS); run reconcile — it will finish the integration or report the repair needed — do not reopen'); } }`. Comment block above the guard (accurate, no overclaim): refuse only a GENUINE this-lifecycle landed CAS (candidate reachable from target + lock held + candidate descends from expected_target = expected_target + reviewed content); a stale leftover whose target ADVANCED is a strict ancestor of expected_target -> proceeds to a cleaning reopen; KNOWN GAP: a leftover that still EQUALS expected_target (target never advanced) is indistinguishable from a landed empty-effect CAS and is still refused (reconcile also refuses at its source-HEAD check); refusal routes to reconcile which completes the genuine row via markIntegrated. This comment wording supersedes the design doc's illustrative block.", "expected": ""},
        {"description": "Verify GREEN + no regression (core + recovery).", "command": "cd /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/workspace-manager && npx vitest run src/__tests__/integration-core.test.ts src/__tests__/integration-recovery.test.ts", "expected": "0 failed (leftover case proceeds; lineage-119 genuine cases + all others still pass)."},
        {"description": "Stage.", "command": "git -C /Users/roberthyatt/Code/ironclaude add worker/mcp-servers/workspace-manager/src/integration.ts worker/mcp-servers/workspace-manager/src/__tests__/integration-cases.ts", "expected": "Staged."}
      ]
    },
    {
      "id": 2,
      "name": "Rebuild dist + full-suite verification",
      "description": "Rebuild dist (Task 1 changed integration.ts), run full vitest + full commander pytest (both 0 failed), stage dist with git add -f.",
      "allowed_files": ["worker/mcp-servers/workspace-manager/dist/cli.js", "worker/mcp-servers/workspace-manager/dist/index.js", "worker/mcp-servers/workspace-manager/dist/hook-intent.js"],
      "depends_on": [1],
      "steps": [
        {"description": "Rebuild dist.", "command": "cd /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/workspace-manager && npm run build", "expected": "tsc && bundle, no error."},
        {"description": "Full workspace-manager vitest.", "command": "cd /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/workspace-manager && npx vitest run", "expected": "passed | 0 failed."},
        {"description": "Full commander pytest.", "command": "cd /Users/roberthyatt/Code/ironclaude/commander && PYTHONUNBUFFERED=1 .venv/bin/python -m pytest -q", "expected": "0 failed."},
        {"description": "Stage dist (force).", "command": "git -C /Users/roberthyatt/Code/ironclaude add -f worker/mcp-servers/workspace-manager/dist/cli.js worker/mcp-servers/workspace-manager/dist/index.js worker/mcp-servers/workspace-manager/dist/hook-intent.js", "expected": "Staged."}
      ]
    }
  ]
}
