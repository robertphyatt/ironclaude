{
  "name": "MCP Subprocess Teardown Verification (investigation-only)",
  "goal": "Verify by code trace + live process census whether the daemon's worker-reap path tears down a reaped worker's MCP subprocess tree; record verdict + non-implemented recommendations in a findings note. No source changes.",
  "requirements_file": "docs/plans/2026-09-16-mcp-teardown-investigation-design.md",
  "design_file": "docs/plans/2026-09-16-mcp-teardown-investigation-design.md",
  "estimated_memory_gb": 0.5,
  "tasks": [
    {
      "id": 1,
      "name": "Q1 worker-teardown code trace (create findings note)",
      "description": "Read-only trace of the worker-teardown terminal paths (incl. the 3629 comm-profile-failure kill site); create the findings note with a Q1 section stating per path whether it kills the tmux session (SIGHUP the pane's foreground group) or leaves the process tree alive (e.g. idle done-marker path). No tests required: read-only investigation producing documentation.",
      "allowed_files": ["commander/docs/plans/2026-09-16-mcp-teardown-findings.md"],
      "depends_on": [],
      "steps": [
        {"description": "Read main.py:3966-4030 (_confirm_and_kill_stuck_worker, tmux.kill_session ~4009); main.py:3617-3631 (comm-profile activation failure -> kill_session ~3629); main.py:4190-4290 (done-marker completion: idle -> _finalize_and_release_worker(terminal=False), session NOT killed ~4224-4225; and session-died ~4243-4280); main.py:1170-1190 (_kill_orphan_workers ~1180, _kill_orphan_brains ~1186); main.py:1540-1557 + orchestrator_mcp.py:3522 (_finalize_and_release_worker). Confirm per path whether it kills the tmux session or only marks DB / reaps worktree / finalizes without a kill."},
        {"description": "Confirm the kill_session method exists.", "command": "grep -n \"def kill_session\" /Users/roberthyatt/Code/ironclaude/commander/src/ironclaude/tmux_manager.py", "expected": "352:    def kill_session (non-empty)."},
        {"description": "Confirm what tmux kill-session runs; record the pty-hangup -> SIGHUP-to-foreground-process-group premise (whether MCP procs die depends on being IN that group, verified empirically in Task 3 via pgid/tty).", "command": "grep -n \"kill-session\" /Users/roberthyatt/Code/ironclaude/commander/src/ironclaude/tmux_manager.py", "expected": "355: line showing tmux kill-session -t (non-empty)."},
        {"description": "Create commander/docs/plans/2026-09-16-mcp-teardown-findings.md with title + 'Q1 - worker-teardown propagation' section: per terminal path, tmux.kill_session (SIGHUP the pane foreground group -> worker + in-group MCP procs die by node default SIGHUP action) vs mark-row/reap-worktree/finalize-without-kill (tree survives, e.g. idle done-marker path). Each claim cites main.py:<line> verified above."},
        {"description": "Stage the findings note (docs/ gitignored -> add -f).", "command": "git -C /Users/roberthyatt/Code/ironclaude add -f commander/docs/plans/2026-09-16-mcp-teardown-findings.md", "expected": "Staged; professional mode blocks commit."}
      ]
    },
    {
      "id": 2,
      "name": "Q2 wrapper + server self-exit-on-parent-death trace",
      "description": "Read-only trace: confirm all three wrappers fork (spawn) and set CLAUDE_PPID; determine per server whether it self-exits on stdin EOF (SDK transport close) or via a CLAUDE_PPID watchdog, or depends on a signal. episodic-memory child is dist/mcp-server.js and its transport entrypoint is src/mcp-server.ts. Uses repeated -e patterns (no backslash-pipe; ugrep 7.8.4 in the executing stage). Append Q2 section. No tests required: read-only investigation producing documentation.",
      "allowed_files": ["commander/docs/plans/2026-09-16-mcp-teardown-findings.md"],
      "depends_on": [1],
      "steps": [
        {"description": "Confirm all three wrappers spawn (fork) the bundle and set CLAUDE_PPID.", "command": "grep -n -e \"spawn\" -e \"execPath\" -e \"CLAUDE_PPID\" /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/workspace-manager/cli/mcp-server-wrapper.js /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/state-manager/cli/mcp-server-wrapper.js /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/episodic-memory/cli/mcp-server-wrapper.js", "expected": "Each wrapper shows spawn(process.execPath,...) + CLAUDE_PPID: workspace-manager spawn ~:36 CLAUDE_PPID :37; state-manager spawn ~:177 CLAUDE_PPID :180; episodic-memory spawn ~:210 CLAUDE_PPID :213 (non-empty all three; fork not exec)."},
        {"description": "Search each server SOURCE (src/) for a stdin-EOF / parent-death self-exit; repeated -e patterns.", "command": "grep -rn -e \"process.stdin\" -e \"CLAUDE_PPID\" -e \"stdin.on\" -e \"on('end')\" -e 'on(\"end\")' -e \"process.ppid\" /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/workspace-manager/src /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/state-manager/src /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/episodic-memory/src", "expected": "Measured against current source (re-confirm at run): workspace-manager CLAUDE_PPID src/index.ts:541; state-manager CLAUDE_PPID src/index.ts:167,172 + src/tools/read-tools.ts:651,654, process.ppid read-tools.ts:651, stdin.on only in advisor-review (child stdin); episodic-memory ZERO hits on all six."},
        {"description": "Read each server transport-connect entrypoint + any CLAUDE_PPID use: workspace-manager src/index.ts:582 (transport) / :541 (ppid); state-manager src/index.ts:180 (transport) / :167,172 (ppid); episodic-memory src/mcp-server.ts:332 (transport; its src/index.ts is a library index with NO transport - do not read that for teardown). Confirm whether the process exits on transport close (stdin EOF) or keeps the loop alive, and whether CLAUDE_PPID drives a self-exit watchdog. Cite file:line."},
        {"description": "Append 'Q2 - self-exit on parent death' section: per server, self-exit on stdin EOF (SDK transport close) and/or CLAUDE_PPID watchdog vs depends-on-signal. State the leak switch: a server that neither self-exits on stdin EOF nor watches CLAUDE_PPID lingers if its parent dies without signalling it (SIGKILL / reparent to launchd). Cite each file:line."},
        {"description": "Stage the findings note.", "command": "git -C /Users/roberthyatt/Code/ironclaude add -f commander/docs/plans/2026-09-16-mcp-teardown-findings.md", "expected": "Staged."}
      ]
    },
    {
      "id": 3,
      "name": "Q3 live survivor census + Q4 fork multiplier",
      "description": "Read-only live census: snapshot all MCP node procs (index.js AND mcp-server.js children) with pid/ppid/pgid/tty, attribute each to a root, join to tmux panes, list tmux sessions + worker DB rows (correct column id, discovered DB path, sqlite3 -readonly), and determine LEAK/NO-LEAK by finding any local MCP proc whose owning tmux session/worker row is gone but the process survives. Count wrapper vs child procs (Q4 multiplier). NO process is signalled or killed. Bracket-escaped ps|grep patterns avoid self-match. No tests required: read-only census producing documentation.",
      "allowed_files": ["commander/docs/plans/2026-09-16-mcp-teardown-findings.md"],
      "depends_on": [2],
      "steps": [
        {"description": "Snapshot every MCP node proc (wrappers + index.js children + episodic-memory mcp-server.js children) with pid/ppid/pgid/tty; bracket-escaped patterns avoid grep self-match.", "command": "ps -Ao pid,ppid,pgid,tty,stat,comm,args | grep -e \"[m]cp-server-wrapper.js\" -e \"[m]cp-servers/.*/dist/index.js\" -e \"[m]cp-servers/.*/dist/mcp-server.js\" > /private/tmp/claude-502/-Users-roberthyatt-Code-ironclaude/7c7f63a0-e272-4e34-a05b-3d6ebc128c94/scratchpad/mcp-census-raw.txt; cat /private/tmp/claude-502/-Users-roberthyatt-Code-ironclaude/7c7f63a0-e272-4e34-a05b-3d6ebc128c94/scratchpad/mcp-census-raw.txt", "expected": "One line per MCP node proc (wrappers + index.js children + mcp-server.js children) with pid/ppid/pgid/tty, or empty if none running. Record verbatim."},
        {"description": "Resolve each distinct PPID to its root; bracket-escaped patterns.", "command": "ps -Ao pid,ppid,pgid,tty,comm,args | grep -i -e \"[c]laude\" -e \"[c]odex\" -e \"[t]mux\" -e \"[i]term\" -e \"[i]ronclaude\"", "expected": "Candidate root/parent processes; classify each MCP proc's root as claude/codex worker, interactive operator session (iTerm/tmux), Codex/ChatGPT app, daemon, or PID 1 (launchd orphan)."},
        {"description": "Join MCP procs to tmux panes (makes Q1 measurable): which MCP procs are actually in a worker pane's process group.", "command": "tmux list-panes -a -F \"#{session_name} #{window_index}.#{pane_index} tty=#{pane_tty} pane_pid=#{pane_pid} cmd=#{pane_current_command}\"", "expected": "One line per pane with tty + pane_pid, or 'no server running'. Join to Step 1 by tty/pgid."},
        {"description": "List live tmux sessions (incl. any ic-* workers).", "command": "tmux list-sessions", "expected": "Current tmux sessions, or a 'no server running' message. Record verbatim."},
        {"description": "Locate worker DB - config default (do not hard-code).", "command": "grep -n \"db_path\" /Users/roberthyatt/Code/ironclaude/commander/src/ironclaude/config.py", "expected": "42:    \"db_path\": \"data/db/ironclaude.db\", plus a DB_PATH env-override mapping (~:96)."},
        {"description": "Locate worker DB - env override actually in effect (sourced by make run).", "command": "grep -n \"DB_PATH\" /Users/roberthyatt/Code/ironclaude/commander/.env", "expected": "8:DB_PATH=data/db/ironclaude.db (relative to commander/), or a different value to substitute below."},
        {"description": "Confirm no JSON override.", "command": "grep -n \"db_path\" /Users/roberthyatt/Code/ironclaude/commander/config/ironclaude.json", "expected": "No output (no override)."},
        {"description": "Confirm the live DB file (non-zero, recent WAL).", "command": "ls -la /Users/roberthyatt/Code/ironclaude/commander/data/db/", "expected": "ironclaude.db non-zero size with recent -wal/-shm siblings; ic.db/tron.db are stale and NOT the target."},
        {"description": "Confirm schema reachable read-only (-readonly errors instead of creating a file on a wrong path).", "command": "sqlite3 -readonly /Users/roberthyatt/Code/ironclaude/commander/data/db/ironclaude.db \".tables\"", "expected": "Table list including workers. If the greps above showed a different db_path, substitute it here and below; do not invent one."},
        {"description": "Read worker rows - status breakdown (PK is id, not worker_id).", "command": "sqlite3 -readonly -header /Users/roberthyatt/Code/ironclaude/commander/data/db/ironclaude.db \"SELECT status, COUNT(*) FROM workers GROUP BY status;\"", "expected": "A status->count breakdown."},
        {"description": "Read worker rows - running set with machine (remote ssh_host workers' tmux lives on another box; do NOT count as local leak). For each ic-* session absent from the running set, look it up by tmux_session (a finished/absent row whose MCP procs still live is the leak signal).", "command": "sqlite3 -readonly -header /Users/roberthyatt/Code/ironclaude/commander/data/db/ironclaude.db \"SELECT id, type, machine, client, tmux_session, status, spawned_at, finished_at, workspace_path FROM workers WHERE status='running';\"", "expected": "Running-worker rows with id, machine, tmux_session. Record verbatim."},
        {"description": "Cross-check: for each MCP proc, are its root (Step 2), tmux pane (Step 3), tmux session (Step 4), and worker row (Step 10) present? Identify any local MCP proc whose owning tmux session/worker row is gone but the process survives (flips no-leak -> leak). Count wrapper vs child procs (index.js + mcp-server.js) for the Q4 2x/server, 6x/session multiplier. Append 'Q3 - live survivor census + Q4 - fork multiplier' section with verbatim snapshots, per-root attribution, MEASURED counts (not predicted), and the explicit leak/no-leak determination + its falsifier. Stage.", "command": "git -C /Users/roberthyatt/Code/ironclaude add -f commander/docs/plans/2026-09-16-mcp-teardown-findings.md", "expected": "Staged."}
      ]
    },
    {
      "id": 4,
      "name": "Verdict + recommendations (consolidate)",
      "description": "Consolidate Q1-Q3 into a mechanical verdict (LEAK CONFIRMED / NO LEAK / CONDITIONAL LEAK with the exact condition) and a prioritized recommendation list drawn only from confirmed findings; NOT implemented here. Record the design-doc dist/index.js prose inaccuracy for episodic-memory. No tests required: documentation consolidation.",
      "allowed_files": ["commander/docs/plans/2026-09-16-mcp-teardown-findings.md"],
      "depends_on": [3],
      "steps": [
        {"description": "Append a 'Verdict' section: LEAK CONFIRMED (surviving proc + missing root/session/row), NO LEAK (teardown verified across all terminal paths + self-exit confirmed), or CONDITIONAL LEAK (exact condition, e.g. a SIGKILL'd worker or an idle done-marker exit that does not kill the session, whose episodic-memory server lacks stdin-EOF self-exit). Must follow mechanically from Q1-Q3 evidence."},
        {"description": "Append 'Recommendations (not implemented - each its own operator-gated loop)': exec-not-fork wrapper (if Q4 confirms 2x); stdin-EOF/CLAUDE_PPID watchdog self-exit for any server Q2 found lacking one (episodic-memory prime candidate); replace the two pgrep -f pattern-kill sites (_kill_orphan_brains main.py:~1186, _cleanup_zombie_mcp_processes orchestrator_mcp.py:~6706) with PID-scoped teardown per the never-pattern-kill directive; a daemon maintenance census surfacing row-less MCP survivors (mirrors v1.1.11 row-less worktree reaper) only if Q3 shows survivors possible. Also record the design-doc prose inaccuracy: it says all three children run dist/index.js but episodic-memory runs dist/mcp-server.js (doc note, not a defect). Each recommendation cites the finding that motivates it; unmotivated ones omitted."},
        {"description": "Stage the completed note (loop deliverable; no source changed).", "command": "git -C /Users/roberthyatt/Code/ironclaude add -f commander/docs/plans/2026-09-16-mcp-teardown-findings.md", "expected": "Staged."}
      ]
    }
  ]
}
