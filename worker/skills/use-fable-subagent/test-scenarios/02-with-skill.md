# GREEN: With Skill

Codex loads lossless AI-message policy, constructs complete report-only packet,
uses bundled launcher, and accepts report only after effective assistant
`message.model` proves Claude Fable. Parent keeps orchestration and repository
authority, independently verifies findings, and performs no fallback.

Automated tests use local fake children; never invoke live Claude.

