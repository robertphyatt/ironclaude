# RED: Without Skill

Given explicit request for actual Fable subagent, baseline Codex collaboration
surface offers only Codex models. Agent may incorrectly equate highest Codex
tier with Fable, use Commander worker label, or claim requested CLI alias proves
effective model. RED result: request is silently substituted or unverifiable.

These are documentation-level scenarios. Never invoke live Claude in tests.

