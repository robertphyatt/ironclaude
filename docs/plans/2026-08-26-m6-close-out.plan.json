{
  "name": "M6 close-out human verb",
  "goal": "Add the /close-out human verb (verb 4): integrate the managed worktree HEAD into local main then fully tear the worktree down, auto-resolving push-pending obligations (carry forward), dirty trees (recovery-ref snapshot), and mechanically-recoverable paused rebases (handler-orchestrated auto-continue), never handing work back to the operator.",
  "requirements_file": "docs/plans/2026-08-26-m6-close-out-requirements.md",
  "design_file": "docs/plans/2026-08-26-m6-close-out-design.md",
  "estimated_memory_gb": 0.5,
  "tasks": [
    {
      "id": 1,
      "name": "Evidence-light close-out intent wiring + closed-out result state",
      "description": "close-out is a managed-only operation whose intent mints lifecycle-tolerantly and evidence-light (branch identity from assignment.branch, no HEAD read), verified by observing live ReconcileEvidence and consuming on the branch-only evidence. FinalizationResult gains 'closed-out' + optional pendingPush/recovery.",
      "allowed_files": ["worker/mcp-servers/workspace-manager/src/git-authority.ts", "worker/mcp-servers/workspace-manager/src/hook-intent.ts", "worker/hooks/state-activator.sh", "worker/mcp-servers/workspace-manager/src/integration.ts", "worker/mcp-servers/workspace-manager/src/__tests__/integration-cases.ts"],
      "depends_on": [],
      "steps": [
        {"description": "RED: integration-cases.ts test — seed an integrated row (seedIntegratedPushPending :147); issue a close-out intent + verify a close-out authority; the intent MUST mint despite lifecycle=integrated, verify observes live HEAD; assert operation==='close-out', checkoutMode==='managed'. Negative: a /reconcile intent on that integrated row still fails (lifecycle-tolerant lookup is close-out-scoped).", "command": "npm --prefix /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/workspace-manager test -- --testTimeout=30000 -t \"close-out\"", "expected": "FAIL — close-out not admitted."},
        {"description": "GREEN: git-authority.ts:7 add | 'close-out' to DirectGitOperation. Add closeOutIntentEvidence(assignment): ReconcileEvidence = {checkoutMode:'managed', canonicalBranch:assignment.branch, localRef:`refs/heads/${assignment.branch}`, headOid:''} — HEAD-independent match key (headOid '' used by BOTH issue and consume so canonicalJson equality holds; the authority's real headOid is observed live)."},
        {"description": "GREEN: git-authority.ts issueDirectGitHumanIntent — add a close-out branch that issues with expectedEvidence=closeOutIntentEvidence(assignment), NO observeDirectEvidence call (no symbolic-ref on a possibly-detached HEAD). verifyDirectGitAuthority — add a close-out arm: resolve checkout; observe LIVE headOid (rev-parse --verify HEAD^{commit}) into the authority's real ReconcileEvidence; consume via consumeMatchingHumanIntent using closeOutIntentEvidence(assignment) (branch-only key), NOT the live evidence. Extend push-exclusion (:589) to exclude 'close-out'. Do NOT alter the reconcile arm."},
        {"description": "GREEN: hook-intent.ts — add a close-out branch resolving the single assignment by guid WITHOUT the active-only filter at :44 (admit active/ready_for_integration/integrated; close-out-scoped query), then issueDirectGitHumanIntent(operation:'close-out'). Leave :44/:49/:66 for the other verbs UNCHANGED (reconcile/commit still refuse integrated)."},
        {"description": "GREEN: worker/hooks/state-activator.sh:54 add close-out to the for-loop. No tests required (bash verb-list; covered end-to-end once Task 3 lands; TS authority Step-1 tested)."},
        {"description": "GREEN: integration.ts FinalizationResult (:32-44) add | 'closed-out' + pendingPush?:{candidateCommit;remoteUrl;destinationRef} + recovery?:{ref;residualFiles}."},
        {"description": "GREEN: run full suite.", "command": "npm --prefix /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/workspace-manager test -- --testTimeout=30000", "expected": "Step-1 passes; suite green."},
        {"description": "Stage.", "command": "git -C /Users/roberthyatt/Code/ironclaude add worker/mcp-servers/workspace-manager/src/git-authority.ts worker/mcp-servers/workspace-manager/src/hook-intent.ts worker/hooks/state-activator.sh worker/mcp-servers/workspace-manager/src/integration.ts worker/mcp-servers/workspace-manager/src/__tests__/integration-cases.ts", "expected": "staged."}
      ]
    },
    {
      "id": 2,
      "name": "finalizeCloseOut (active/ready-frozen/integrated) + closeOutRelease (clean)",
      "description": "finalizeCloseOut drives an active / ready-frozen-no-rebase / integrated row to integrated and ends in closeOutRelease (clean case), returning closed-out, worktree removed. Paused-rebase recovery is the Task-3 handler; the push-pending carry is Task 4. Leaves releaseFinalized + its :638 throw byte-untouched.",
      "allowed_files": ["worker/mcp-servers/workspace-manager/src/integration.ts", "worker/mcp-servers/workspace-manager/src/__tests__/integration-cases.ts"],
      "depends_on": [1],
      "steps": [
        {"description": "RED: integration-cases.ts — setup(false); commit staged work; mint a close-out authority (Task-1 path); finalizeCloseOut → assert closed-out, integratedCommit===HEAD, root main advanced, row cleaned, worktree removed, branch gone.", "command": "npm --prefix /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/workspace-manager test -- --testTimeout=30000 -t \"close-out\"", "expected": "FAIL — finalizeCloseOut not exported."},
        {"description": "GREEN: integration.ts add closeOutRelease(db, repositoryPath, assignment, recovery?) — clean case: require integrated row + integrated_commit; require worktreeIsClean; reachable-integration proof (duplicate releaseFinalized :644-659 verbatim); removeWorktree + deleteTemporaryBranch + transitionAssignment(integrated→cleaned); return {state:'closed-out', integratedCommit, ...(recovery?{recovery}:{})}. Import worktreeIsClean if needed."},
        {"description": "GREEN: integration.ts add finalizeCloseOut(db, authority) with usedCloseOutAuthorities WeakSet — guards (operation==='close-out'; managed; single-use; revalidateAuthorizedCommitState; exactAssignment; headOid pin === worktreeHead). Branches: active → finalizeLocalCommit → closeOutRelease; ready_for_integration → mirror finalizeReconcile ready branch :1173-1182 (frozen-ref check, update-ref candidate, finalizeAttestedCandidate) then closeOutRelease; integrated → closeOutRelease directly. Terminal throw otherwise. (Task 5 inserts snapshotResidualIfDirty after the pin; Task 4 adds the integrated worktree-gone edge + carry.)"},
        {"description": "GREEN: tests for the ready-frozen and integrated branches (ready frozen-no-rebase row → closed-out; integrated row with NO push-pending → closed-out via the integrated-proof path).", "command": "npm --prefix /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/workspace-manager test -- --testTimeout=30000", "expected": "all pass."},
        {"description": "Stage.", "command": "git -C /Users/roberthyatt/Code/ironclaude add worker/mcp-servers/workspace-manager/src/integration.ts worker/mcp-servers/workspace-manager/src/__tests__/integration-cases.ts", "expected": "staged."}
      ]
    },
    {
      "id": 3,
      "name": "close_out_worktree MCP tool + handler-orchestrated rebase recovery + skill",
      "description": "The close_out_worktree handler probes reconcileFinalization('status'); rebase-paused-conflict returns preserve-and-defer (no authority); rebase-paused-clean is auto-continued via authority-free reconcileFinalization('continue'); then verify authority + finalizeCloseOut. reconcileFinalization is used UNMODIFIED.",
      "allowed_files": ["worker/mcp-servers/workspace-manager/src/index.ts", "worker/skills/close-out/SKILL.md", "worker/mcp-servers/workspace-manager/src/__tests__/integration-cases.ts", "worker/mcp-servers/workspace-manager/src/__tests__/tool-dispatch.test.ts"],
      "depends_on": [2],
      "steps": [
        {"description": "RED: integration-cases.ts (via the handler dependency, mirroring reconcileWorktree tool tests) — a rebase-paused-clean row → handler → closed-out, worktree removed; a rebase-paused-conflict row (seedConflictMidRebase :175) → preserve-and-defer result, worktree PRESERVED, row unchanged, NO operator instruction, NO close-out authority consumed.", "command": "npm --prefix /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/workspace-manager test -- --testTimeout=30000 -t \"close-out\"", "expected": "FAIL — close_out_worktree tool absent."},
        {"description": "GREEN: index.ts add close_out_worktree at 5 sites mirroring reconcile_worktree, with a richer handler: requireProviderRoot; status = reconcileFinalization(db,{...,rebaseRecovery:'status'}); if status.state==='rebase-paused-conflict' return {state:'rebase-paused-conflict', detail:'Close-out paused: a rebase conflict needs automated resolution (pending); worktree preserved. Not an operator task.'}; if status.state==='rebase-paused-clean' reconcileFinalization(db,{...,rebaseRecovery:'continue'}); authority=verifyDirectGitAuthority(db,{...,operation:'close-out'}); return finalizeCloseOut(db, authority). Confirm status return values against integration.ts:1395-1403. Import finalizeCloseOut, reconcileFinalization."},
        {"description": "GREEN: tool-dispatch.test.ts add 'close_out_worktree' to PUBLIC_TOOLS + a closeOutWorktree mock to publicDependencies."},
        {"description": "GREEN: create worker/skills/close-out/SKILL.md by cloning reconcile/SKILL.md — name close-out; /close-out forms; semantics integrate + FULL teardown; step 2 calls close_out_worktree; step 3 success contract names ACTUAL reachable states: closed-out (normal); rebase-paused-conflict and rebase-recovery-repair-required (worktree preserved, pending automated M7, never handed to the operator); a no-active-assignment re-run reports a clear 'nothing to close out' non-error message. Do NOT claim cleaned/integrated-local."},
        {"description": "GREEN: run suite.", "command": "npm --prefix /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/workspace-manager test -- --testTimeout=30000", "expected": "all pass incl. recovery paths."},
        {"description": "Stage.", "command": "git -C /Users/roberthyatt/Code/ironclaude add worker/mcp-servers/workspace-manager/src/index.ts worker/skills/close-out/SKILL.md worker/mcp-servers/workspace-manager/src/__tests__/integration-cases.ts worker/mcp-servers/workspace-manager/src/__tests__/tool-dispatch.test.ts", "expected": "staged."}
      ]
    },
    {
      "id": 4,
      "name": "Case A — carry push-pending obligation + self-heal + worktree-gone edge",
      "description": "closeOutRelease carries a push-pending obligation on the terminal cleaned row (after a read-only ls-remote self-heal); the finalizeCloseOut integrated branch handles the persistent state and the worktree-gone DB-only edge.",
      "allowed_files": ["worker/mcp-servers/workspace-manager/src/integration.ts", "worker/mcp-servers/workspace-manager/src/__tests__/integration-cases.ts"],
      "depends_on": [3],
      "steps": [
        {"description": "RED: three tests — (1) seedIntegratedPushPending (:147) verbatim → finalizeCloseOut integrated branch → closed-out, worktree removed, row cleaned, disposition STILL push-pending, pendingPush returned, refs resolve. (2) intra-call: ACTIVE row + INTEGRATION-PENDING disposition (mirror reconcile integrated-local seed :437-444, no pre-seeded record/refs) → finalizeLocalCommit markIntegrated converts to push-pending → carry. (3) self-heal: like (1) but advance the bare remote to the candidate first → disposition null, pendingPush undefined, no push.", "command": "npm --prefix /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/workspace-manager test -- --testTimeout=30000 -t \"close-out\"", "expected": "FAIL — closeOutRelease has no push-pending branch."},
        {"description": "GREEN: closeOutRelease — after the integrated-row check, before the clean-tree guard: d=decodePushDisposition(current.disposition); if d, try remote=remoteRefOid(current.worktree_path,d.remoteUrl,d.destinationRef) in try/catch (offline non-fatal); if remote===d.candidateCommit setDisposition(null) else pendingPush={candidateCommit,remoteUrl,destinationRef}. Return ...(pendingPush?{pendingPush}:{}). integrated→cleaned preserves the disposition (db.ts:316)."},
        {"description": "GREEN: finalizeCloseOut integrated branch — if !existsSync(worktree_path) complete DB-only: verify durable integration proof (candidate ⊆ main via integration_records), transitionAssignment(integrated→cleaned) carrying disposition, return {state:'closed-out', integratedCommit, pendingPush?} — no removeWorktree. Test: seed integrated row, rmSync its worktree, finalizeCloseOut → closed-out, row cleaned."},
        {"description": "GREEN: run suite + stage.", "command": "npm --prefix /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/workspace-manager test -- --testTimeout=30000", "expected": "all pass."},
        {"description": "Stage.", "command": "git -C /Users/roberthyatt/Code/ironclaude add worker/mcp-servers/workspace-manager/src/integration.ts worker/mcp-servers/workspace-manager/src/__tests__/integration-cases.ts", "expected": "staged."}
      ]
    },
    {
      "id": 5,
      "name": "Case B — dirty-tree recovery-ref snapshot (scratch index, never on main)",
      "description": "A dirty worktree snapshots residual to refs/ironclaude/recovery/<guid> via a temp index in a SCRATCH path OUTSIDE the worktree (so add -A doesn't capture the index), BEFORE cleaning; integration lands only the reviewed HEAD. Most destructive — recovery ref minted AND re-verified before any reset. Adds runGitEnv to git.ts (mirrors runGit's spawnSync, NOT execFileSync).",
      "allowed_files": ["worker/mcp-servers/workspace-manager/src/integration.ts", "worker/mcp-servers/workspace-manager/src/git.ts", "worker/mcp-servers/workspace-manager/src/__tests__/integration-cases.ts"],
      "depends_on": [4],
      "steps": [
        {"description": "RED: integration-cases.ts — setup(false); commit reviewed work; write an untracked stray + tracked modification; finalizeCloseOut → assert closed-out, recovery.ref==='refs/ironclaude/recovery/'+guid, recovery.residualFiles>=1, ref resolves in primary checkout and its tree contains stray.txt, local main HEAD tree EXCLUDES stray.txt, worktree removed, row cleaned with recovery_ref set.", "command": "npm --prefix /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/workspace-manager test -- --testTimeout=30000 -t \"close-out\"", "expected": "FAIL — snapshotResidualIfDirty absent."},
        {"description": "GREEN: git.ts add runGitEnv(cwd, args, env) mirroring runGit's spawnSync (git.ts:28), passing env."},
        {"description": "GREEN: integration.ts add snapshotResidualIfDirty(db, exact): if worktreeIsClean return undefined; residualFiles from status --porcelain=v1 --untracked-files=all; temp GIT_INDEX_FILE under tmpdir() (NOT inside worktree): read-tree HEAD, add -A, write-tree, commit-tree <tree> -p HEAD -m 'ironclaude: close-out residual snapshot' (finally rm the temp index); update-ref refs/ironclaude/recovery/<guid>; UPDATE recovery_ref; rev-parse --verify the ref BEFORE reset --hard HEAD + clean -fd. Import rmSync, join, tmpdir."},
        {"description": "GREEN: finalizeCloseOut — after the headOid pin, before the lifecycle branches: const recovery = snapshotResidualIfDirty(db, exact); thread recovery into every closeOutRelease call. reset --hard HEAD keeps the commit so the pin holds."},
        {"description": "GREEN: run suite + stage.", "command": "npm --prefix /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/workspace-manager test -- --testTimeout=30000", "expected": "all pass incl. Case B."},
        {"description": "Stage.", "command": "git -C /Users/roberthyatt/Code/ironclaude add worker/mcp-servers/workspace-manager/src/integration.ts worker/mcp-servers/workspace-manager/src/git.ts worker/mcp-servers/workspace-manager/src/__tests__/integration-cases.ts", "expected": "staged."}
      ]
    },
    {
      "id": 6,
      "name": "C6 — minimal push-lane drain of stale carried obligations",
      "description": "After a successful /push, clear the stale push disposition on any cleaned row whose carried commit the push satisfied (containment: isAncestor(candidateCommit, pushedLocalOid)). Hygiene not correctness; smallest touch. index.ts is in allowed_files because threading db into finalizePrimaryUnassignedPush changes its signature + call site index.ts:294.",
      "allowed_files": ["worker/mcp-servers/workspace-manager/src/integration.ts", "worker/mcp-servers/workspace-manager/src/index.ts", "worker/mcp-servers/workspace-manager/src/__tests__/integration-cases.ts"],
      "depends_on": [5],
      "steps": [
        {"description": "RED: integration-cases.ts — produce a cleaned row carrying a push-pending disposition for (remoteUrl, destinationRef) whose candidateCommit is an ancestor of local main (reuse the Task-4 Case-A close-out). Run the push-only or unassigned-primary /push of destinationRef whose localOid contains the candidate → assert the cleaned row's disposition becomes null. Negative: a carried obligation for a DIFFERENT destinationRef is left intact.", "command": "npm --prefix /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/workspace-manager test -- --testTimeout=30000 -t \"close-out\"", "expected": "FAIL — carried disposition not cleared by a matching push."},
        {"description": "GREEN: integration.ts add drainCarriedObligations(db, repositoryIdentity, remoteUrl, destinationRef, pushedLocalOid, primaryCheckoutPath): SELECT cleaned rows with non-null disposition for repository_identity; for each decodePushDisposition; if d.remoteUrl===remoteUrl && d.destinationRef===destinationRef && isAncestor(primaryCheckoutPath, d.candidateCommit, pushedLocalOid) then setDisposition(null). Call at BOTH success points before return {state:'pushed-only'}: push-only lane (:1029, has db+exact) and finalizePrimaryUnassignedPush (:954). Thread db into finalizePrimaryUnassignedPush(db, authority, hooks?) — changes signature + call site index.ts:294 (index.ts in files). Derive repositoryIdentity/primaryCheckoutPath via discoverRepository(authority.worktreePath). Verified: only tombstoneTerminalAssignment reads a cleaned-row disposition and handles integrated/abandoned not cleaned — pure hygiene."},
        {"description": "GREEN: run suite + stage.", "command": "npm --prefix /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/workspace-manager test -- --testTimeout=30000", "expected": "all pass; existing push-lane tests unchanged."},
        {"description": "Stage.", "command": "git -C /Users/roberthyatt/Code/ironclaude add worker/mcp-servers/workspace-manager/src/integration.ts worker/mcp-servers/workspace-manager/src/index.ts worker/mcp-servers/workspace-manager/src/__tests__/integration-cases.ts", "expected": "staged."}
      ]
    },
    {
      "id": 7,
      "name": "C7 — observability of carried obligations + recovery refs",
      "description": "Surface carried obligations (push-pending cleaned rows) + recovery refs. Exports a pure pushPendingSummary predicate from integration.ts; adds a listPreservedWork surface wired to a public tool.",
      "allowed_files": ["worker/mcp-servers/workspace-manager/src/integration.ts", "worker/mcp-servers/workspace-manager/src/index.ts", "worker/mcp-servers/workspace-manager/src/workspace-service.ts", "worker/mcp-servers/workspace-manager/src/__tests__/integration-cases.ts", "worker/mcp-servers/workspace-manager/src/__tests__/tool-dispatch.test.ts"],
      "depends_on": [6],
      "steps": [
        {"description": "RED: after a Case-A close-out (cleaned row carrying push-pending) and a Case-B close-out (cleaned row with recovery_ref), a preserved-work query for the repository returns each {workspace_guid, kind:'pending-push'|'recovery', destinationRef?, ref?}.", "command": "npm --prefix /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/workspace-manager test -- --testTimeout=30000 -t \"preserved\"", "expected": "FAIL — no preserved-work surface."},
        {"description": "GREEN: integration.ts export pure pushPendingSummary(disposition: string | null) returning {candidateCommit,remoteUrl,destinationRef}|undefined via decodePushDisposition (no DB)."},
        {"description": "GREEN: add a listPreservedWork surface (WorkspaceService method or inline index.ts query mirroring listActiveAssignments :274) selecting cleaned/abandoned rows for repository+owner whose disposition is push-pending OR recovery_ref non-null, mapped via pushPendingSummary/recovery_ref to {workspace_guid, kind, destinationRef?, ref?}; wire to a public tool (mirror list_active_assignments at the 5 sites + tool-dispatch.test.ts fixtures if a tool is added)."},
        {"description": "GREEN: run suite + stage.", "command": "npm --prefix /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/workspace-manager test -- --testTimeout=30000", "expected": "all pass."},
        {"description": "Stage.", "command": "git -C /Users/roberthyatt/Code/ironclaude add worker/mcp-servers/workspace-manager/src/integration.ts worker/mcp-servers/workspace-manager/src/index.ts worker/mcp-servers/workspace-manager/src/workspace-service.ts worker/mcp-servers/workspace-manager/src/__tests__/integration-cases.ts worker/mcp-servers/workspace-manager/src/__tests__/tool-dispatch.test.ts", "expected": "staged."}
      ]
    }
  ]
}
