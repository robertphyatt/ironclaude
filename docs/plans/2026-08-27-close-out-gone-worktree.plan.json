{
  "name": "Gone-worktree close-out completion (R3), unified with the reaper",
  "goal": "Complete a crash-mid-teardown /close-out (integrated row, worktree gone) DB-only without a raw error, and unify with the reaper so tombstoneTerminalAssignment carries a push-pending obligation into preserved_work instead of refusing.",
  "requirements_file": "docs/plans/2026-08-26-m6-close-out-requirements.md",
  "design_file": "docs/plans/2026-08-27-close-out-gone-worktree-design.md",
  "estimated_memory_gb": 0.5,
  "tasks": [
    {
      "id": 1,
      "name": "tombstoneTerminalAssignment carries the push-pending obligation",
      "description": "Replace the push-pending refusal in tombstoneTerminalAssignment (workspace-service.ts:730-732) with a carry: pushPendingSummary(disposition) -> if defined, insertPreservedWork(pending-push) wrapped in ONE db.transaction with the integrated->cleaned transition (:743). pushPendingSummary is the single source for detection+payload (already excludes push-succeeded). Affects both callers: cleanupWorkspace (owner-bound) and reapLeakedAssignment (reaper). No push. The dirty-worktree guard (:702-704) and integration proof (:714-729) stay intact.",
      "allowed_files": [
        "worker/mcp-servers/workspace-manager/src/workspace-service.ts",
        "worker/mcp-servers/workspace-manager/src/__tests__/workspace-service.test.ts"
      ],
      "depends_on": [],
      "steps": [
        {"description": "RED: In workspace-service.test.ts, REWRITE the :762 test 'refuses to tombstone an integrated worktree with a push-pending obligation' -> 'carries a push-pending obligation into preserved_work when tombstoning an integrated worktree': same seed (integrated push-pending row, PRESENT clean worktree, recordIntegration, merge --ff-only) but assert cleanupWorkspace does NOT throw, row is cleaned, worktree removed (existsSync false), branch gone, and a preserved_work pending-push unresolved row exists with payload candidateCommit===integratedCommit and destinationRef==='refs/heads/main'. ADD a test 'reapLeakedAssignment carries a push-pending dead-worker obligation (integrated + gone worktree)': seed integrated push-pending, git worktree remove --force the worktree, UPDATE assignments SET owner_session_id=NULL (leaked), reapLeakedAssignment -> cleaned + preserved_work pending-push row.", "command": "npm --prefix /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/workspace-manager test -- --testTimeout=30000 -t \"carries a push-pending\"", "expected": "FAIL - currently throws 'push-pending obligation'."},
        {"description": "GREEN: workspace-service.ts imports += insertPreservedWork (from ./db.js, :5-17) and pushPendingSummary (from ./integration.js, :18). In tombstoneTerminalAssignment DELETE the refusal block at :730-732 (the hasPushPendingObligation throw). Replace the final :743 `return transitionAssignment(this.db, assignment.workspace_guid, assignment.lifecycle_status, 'cleaned');` with: const carried = pushPendingSummary(assignment.disposition); return this.db.transaction(() => { const cleaned = transitionAssignment(this.db, assignment.workspace_guid, assignment.lifecycle_status, 'cleaned'); if (carried) insertPreservedWork(this.db, { workspaceGuid: assignment.workspace_guid, repositoryIdentity: repository.repositoryIdentity, ownerSessionId: assignment.owner_session_id, kind: 'pending-push', payload: JSON.stringify(carried) }); return cleaned; })(); If deleting the refusal leaves hasPushPendingObligation unused, remove it from the import so tsc passes."},
        {"description": "Run full suite (no -t filter).", "command": "npm --prefix /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/workspace-manager test -- --testTimeout=30000", "expected": "all pass."},
        {"description": "Typecheck then stage.", "command": "cd /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/workspace-manager && npx tsc --noEmit && git -C /Users/roberthyatt/Code/ironclaude add worker/mcp-servers/workspace-manager/src/workspace-service.ts worker/mcp-servers/workspace-manager/src/__tests__/workspace-service.test.ts", "expected": "tsc exit 0; staged."}
      ]
    },
    {
      "id": 2,
      "name": "closeOutWorktree handler routes the gone-worktree case",
      "description": "In the closeOutWorktree handler (index.ts:354), BEFORE the reconcileFinalization('status') probe, detect an integrated row whose managed worktree is absent (fs.existsSync(worktree_path) && worktreeExists(primary, worktree_path) === false) and route to service.cleanupWorkspace({repositoryPath, workspaceGuid, ownerSessionId: identity.sessionId}) (owner-bound; now carries via Task 1), mapping the returned Assignment to {state:'closed-out', integratedCommit, pendingPush?: pushPendingSummary(pre-cleanup disposition)}. A PRESENT worktree still takes the existing status-probe -> authority -> finalizeCloseOut path. resolveEffectiveCheckout / finalizeCloseOut / closeOutRelease UNCHANGED.",
      "allowed_files": [
        "worker/mcp-servers/workspace-manager/src/index.ts",
        "worker/mcp-servers/workspace-manager/src/__tests__/integration-cases.ts"
      ],
      "depends_on": [1],
      "steps": [
        {"description": "RED: In integration-cases.ts add 'close_out_worktree handler completes DB-only on an integrated row whose worktree is gone (R3)': seedIntegratedPushPending(s), git(s.root,'worktree','remove','--force',s.assignment.worktree_path), issueDirectGitHumanIntent(... operation:'close-out') (mirror the existing active-row handler test), createPublicToolDependencies(db,{client:'codex',sessionId:OWNER,invocationThreadId:OWNER,source:'codex_meta'}), deps.closeOutWorktree({repository_path:s.root, workspace_guid:s.assignment.workspace_guid}); assert state==='closed-out', pendingPush matches {candidateCommit: seeded.candidate, destinationRef: seeded.destinationRef}, row cleaned, a preserved_work pending-push row exists, and NO 'managed workspace Git identity does not match' throw. ADD 'close_out_worktree handler on an integrated PRESENT worktree still uses the normal path': seedIntegratedPushPending(s) (present), issue the close-out intent, call handler -> state==='closed-out', row cleaned, AND the discriminating assertion only the normal path satisfies: the close-out human intent was CONSUMED -- const intent = s.database.prepare(\"SELECT consumed_at FROM human_intents WHERE workspace_guid = ? AND operation = 'close-out'\").get(s.assignment.workspace_guid) as { consumed_at: string | null }; expect(intent.consumed_at).not.toBeNull(); The normal finalizeCloseOut path consumes via consumeMatchingHumanIntent (git-authority.ts:609-611); the DB-only fast path never touches human_intents, so a present-predicate regression (fast path firing on a live worktree) fails here (state==='closed-out'+cleaned alone cannot discriminate -- both paths converge post-carry).", "command": "npm --prefix /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/workspace-manager test -- --testTimeout=30000 -t \"worktree is gone (R3)\"", "expected": "FAIL - handler throws a raw identity/worktree error on the gone worktree, not closed-out."},
        {"description": "GREEN: index.ts imports += getAssignment (from ./db.js, :12) and worktreeExists (from ./git.js, :14). In closeOutWorktree, right after `const workspaceGuid = requiredString(args, 'workspace_guid');` and BEFORE the reconcileFinalization('status') probe, insert: const existing = getAssignment(db, workspaceGuid); if (existing && existing.lifecycle_status === 'integrated') { const repo = discoverRepository(repositoryPath); const present = fs.existsSync(existing.worktree_path) && worktreeExists(repo.primaryCheckoutPath, existing.worktree_path); if (!present) { const carried = pushPendingSummary(existing.disposition); const cleaned = service.cleanupWorkspace({ repositoryPath, workspaceGuid, ownerSessionId: identity.sessionId }); return { state: 'closed-out', integratedCommit: cleaned.integrated_commit ?? existing.integrated_commit ?? undefined, ...(carried ? { pendingPush: carried } : {}) }; } }", "expected": "handler routes gone-worktree integrated rows to cleanupWorkspace."},
        {"description": "Run full suite.", "command": "npm --prefix /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/workspace-manager test -- --testTimeout=30000", "expected": "all pass incl. both new handler tests."},
        {"description": "Typecheck then stage.", "command": "cd /Users/roberthyatt/Code/ironclaude/worker/mcp-servers/workspace-manager && npx tsc --noEmit && git -C /Users/roberthyatt/Code/ironclaude add worker/mcp-servers/workspace-manager/src/index.ts worker/mcp-servers/workspace-manager/src/__tests__/integration-cases.ts", "expected": "tsc exit 0; staged."}
      ]
    }
  ]
}
