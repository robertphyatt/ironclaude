# IronClaude v1.1.0 Overall Roadmap

**Date:** 2026-07-20
**Release objective:** One complete v1.1.0 release with native Claude/Codex Commander parity, explicit configuration, bidirectional automatic failover, durable operator-visible behavior, and no surprise routing.
**Requirements authority:** `docs/plans/2026-07-19-multi-provider-v1-1-requirements.md`

## Operating Rules

1. Every implementation wave is its own complete brainstorm → write-plans → execute-plans PM loop.
2. Operator guidance remains authoritative. Each loop updates the requirements ledger only through operator-visible brainstorming and explicit approval.
3. A blind plan review receives only the approved requirements, design, human plan, and machine plan. It verifies requirements → design, design → plan, then technical executability.
4. First verified `HAS-ISSUES` result triggers one holistic invariant audit. Requirements/design conflicts retreat to brainstorming; plan-only defects produce one coherent human/machine plan regeneration and a fresh reviewer.
5. Reviewer findings are evidence, not authority. Source verification precedes any correction.
6. Each wave lands focused tests and full-suite evidence. No later wave may weaken an earlier acceptance gate.
7. Do not push until the operator explicitly directs it. Final history is squashed only after every release wave and final audit pass.

## Wave Map

| Wave | PM loop | Requirements / queued item | Outcome and release gate |
|---|---|---|---|
| 0 | Review convergence prerequisite | MP-W01–MP-W07, MP-W09, MP-W10 | **Source-complete and hot-deployed.** Requirements-aware blind packet, first-failure holistic convergence, source verification, coherent plan regeneration, exact verdict gate, focused/full tests, and installed-cache parity. Full process restart plus verified same-task activation is the process boundary; replacement-task recovery is conditional. |
| 1 | Workflow transition idempotency | MP-W08 | Inventory every state-transition caller and handler. Callers preflight current state and skip same-target transitions. State manager returns side-effect-free `changed: false` for defensive same-target requests. Focused transition/audit tests and full regression pass. |
| 1R | Codex runtime activation and same-task restart | MP-W12 | Capture provider-specific manifest and bundle identity once at MCP startup. After an IronClaude self-update, apply cachebuster before final build, reinstall, fully restart Codex, reopen the same native task, verify complete runtime fingerprint and installed-cache hashes, then require a normal different-stage transition to return `changed:true`. Missing/mismatched evidence fails closed; a replacement task is recovery-only. |
| 2 | Provider foundations | MP-001, MP-002, MP-011, MP-012, MP-D04, MP-C01 | Define explicit host/client/role/tier capability configuration; native Codex subscription authentication; Luna/Terra/Sol tier mapping; independent Claude-only, Codex-only, or dual-client SSH hosts. No API keys, API billing, translation layer, or Claude aliases. |
| 3 | Native role routing | MP-003, MP-006, MP-D01, MP-D05, MP-D10 | Route Brain, workers, graders, advisors, and applicable helpers at request/spawn time. Preserve native Fable behavior and degrade Fable → Opus before cross-client Sol. Verify local and SSH execution for each configured role/tier. |
| 4 | Automatic failover and conversation continuity | MP-004, MP-007, MP-009, MP-010, MP-C02, MP-C03 | Explicitly configured dual-client roles automatically fail over in both directions for verified infrastructure unavailability. Flip each eligible alternative once before waiting. Start a new native destination conversation with original prompt, raw source-client conversation log, workspace, and continuation instruction. Keep assignments sticky; never silently fail back. |
| 5 | Slack controls and signaling | MP-005, MP-008, MP-D02, MP-D03, MP-D06, MP-D07, MP-C04 | Add status, global swap, per-role swap, recovery, outage/exhaustion, and verified-recovery feedback. Every failover creates a new standalone Slack message and pins it; no shared message tracking, updating, replacing, or unpinning. Suppress duplicate unchanged outage noise. |
| 6 | Shadow mode and remote resilience | MP-D08, MP-D09, MP-C05 | Add opt-in observational shadow comparison with default off and no influence on routing or artifacts. Exercise forced limits, client failures, both-exhausted waiting, recovery, remote/SSH capability combinations, and end-to-end failover. |
| 7 | Codex professional-mode UX and hook compatibility | Queued Codex deactivation defect and Codex status-parity requirement | Teach hooks to recognize Codex `$ironclaude:` skill-link prompts as well as slash commands. Make documented session-variable fallback provider-aware (`CODEX_THREAD_ID` versus Claude identity). Verify activate/deactivate audit events through both syntaxes without weakening human-only deactivation. Expose clear IronClaude version, professional-mode on/off state, workflow status, and native task identity in the Codex-supported status surface. |
| 8 | Codex episodic-memory completeness | Queued memory validation | Prove Codex conversations are captured completely enough for `remembering-conversations`: root task identity, user/assistant/tool events, compaction continuity, concurrent-task isolation, and retrieval. Repair only evidence-backed gaps and add regression/operational validation. |
| 9 | Required-skill availability and safe fallback policy | Queued commit-protocol defect | Determine why Codex reported the required commit-protocol skill unavailable. Ensure required workflow skills are packaged/discoverable. Prohibit an agent from inventing an unauthorized substitute workflow when a mandatory skill is absent; produce a clear blocking/recovery path instead. |
| 10 | Read-only Git guard hardening | MP-C06 | Add anchored plain `git check-ignore` to the read-only Git allowlist while preserving protections against chaining, redirection, process substitution, and preceding-command bypasses at every professional stage. |
| 11 | MCP `create_plan` call-shape hardening | Queued `create_plan` defect | Prevent the missing `plan_json` wrapper failure through callable-schema guidance, skill examples, and focused dispatch/contract tests for `create_plan({ plan_json: <PlanJson> })`. Never invent a retry protocol that bypasses the hard gate. |
| 12 | Release integration and documentation | MP-013, MP-014, MP-C07 | Integrate all completed waves; update README, CHANGELOG, plugin metadata, manifests, version, configuration/migration guidance, status output, and direct-versus-Commander support language for v1.1.0. Confirm durable cross-compaction continuation and no surprise defaults. |
| 13 | Release verification and history | All active requirements, especially MP-D08 | Run requirement-by-requirement acceptance audit, focused suites, full Commander/state-manager/plugin suites, local install/hash verification, and adversarial final review. Any verified defect creates a separately scoped remediation PM wave before this gate is rerun. Squash all release work into the v1.1.0 commit only after the release is complete; do not push until explicit operator direction. |

## Dependency Sequence

```text
Wave 0 review convergence
  -> Wave 1 transition idempotency
  -> Wave 1R runtime activation and same-task restart
  -> Wave 2 provider foundations
  -> Wave 3 native role routing
  -> Wave 4 failover and continuity
  -> Wave 5 Slack control/signaling
  -> Wave 6 shadow and remote resilience

Waves 7-11 are bounded corrective PM loops. They may run after Wave 1 when their
dependencies are available, but each must complete before Wave 12 integration.

Waves 2-11 complete
  -> Wave 12 release integration/docs
  -> Wave 13 final verification/squash
```

## Cross-Wave Release Gates

- **Configuration safety:** Detection never enables routing. Dual-client failover activates automatically only after the operator explicitly configures both eligible clients for the role.
- **Identity safety:** Preserve MP-W11 provider-native root identity and interleaved-task isolation in every hook, MCP, provider, SSH, memory, and failover change.
- **Continuity:** Cross-client handoff uses raw native conversation history plus continuation context; no provider-neutral transcript subsystem.
- **Operator visibility:** Manual switches, automatic failover, exhaustion, waiting, and verified recovery are explicit in Slack.
- **No silent degradation:** Both-client exhaustion waits visibly; missing mandatory workflow capability blocks with an actionable error rather than an invented fallback.
- **No surprise behavior:** Shadow mode defaults off; recovery does not silently fail back; Fable semantics remain native; provider selection is explicit and sticky.
- **Evidence:** Each PM loop records focused regression results, affected full-suite results, built artifacts, and installed-cache parity when hot-deployed.

## Immediate Next Action

After an IronClaude self-update, fully quit and relaunch Codex, reopen the same
native task, verify the provider-specific runtime fingerprint against the
intended installed cache, and require one valid different-stage transition to
return `changed:true`. Only use a replacement task if that verification fails.
Then continue with the next named roadmap wave as its own PM loop; do not combine
provider implementation or queued corrective items across loops.

## Post-v1.1.1 Corrective Waves

> **Note:** this roadmap's title and release objective still say v1.1.0. v1.1.1 shipped
> 2026-07-31 (tag `v1.1.1`, commit `cd0664d`). The stale framing is recorded here rather
> than corrected in place; correcting it is its own loop.

| Wave | PM loop | Item | Outcome and gate |
|---|---|---|---|
| 14 | Fable-flag test isolation | Live defect: the commander suite wrote the operator's real `~/.ironclaude/state/fable_unavailable.json`, planting a 24h Fable blackout that silently downgraded tier-up plan reviews (occurrences 2026-07-17, 07-21, 07-30). Cause: `test_brain_client.py:211` injected a mocked model-unavailable error and the real write path ran unisolated. | Autouse `_isolate_fable_state` fixture in `commander/tests/conftest.py`. Gate: positive control proved the flag reappears without the fixture (`rc=0`) and stays absent with it (`rc=1`) across a full suite run; 2436 passed / 0 failed before and after. **Done.** |
| 15 | Generalization-blindness detector | Recurring failure mode: a claim verified against **one** instance and generalized to the class. It produced two wrong verdicts in the 2026-07-30/31 investigation — a positive control that could not detect its own blind spot, and "the test suite is hermetic" concluded from one of several test files. | Add the archetype to the plan-review and code-review contracts as a **reviewer-side detector**, not an author-side warning: flag any claim of class-wide coverage whose evidence is a single instance. Not started. |
| 16 | Other real-path test isolation | Real-path resolvers in `commander/src/ironclaude/` reachable from tests, surfaced by Wave 14's three-idiom sweep (`Path.home()`, `expanduser`, `HOME`). Recorded, not fixed. | Scope from the Wave 14 findings before designing. Wave 16 migration (Tasks 1-4) added `paths.py` and converted four frozen import-time constants; full-suite verification (Task 5) confirmed `2451 passed`, no `failed`/`error`, and no surviving `RealHomeAccess` trips. **Done**, with `fable_availability._STATE_PATH` deferred — see Wave 17. |
| 18 | **Opus diligence — DISCUSSION FIRST, do not design a fix cold** | Operator directive 2026-08-01, verbatim: *"I want to do a discussion on how to make it so that opus is better at 1) actually looking at my wiki and episodic memory instead of just winging it like a hyperactive over caffeinated adhd kid 2) slowing down and calling it when it doesn't know what my guidance is 3) actually looking at the code 4) calling out when it thinks we should have a fable sub agent double checking, etc. I've been doing it manually when things look fishy and pretty much every time it was needed."* Same-day evidence for all four, session `0e0fa2e6`: **(1)** three consecutive PM loops run off mid-session discoveries while the operator's approved five-item queue sat at zero, with the option menus ranking their own directive last — the queue was in memory throughout; **(2)** offered a menu of design shapes for the Fable-flag loop when the operator had already specified it completely on 2026-07-23 (*"why are your asking about this? I already talked about it a lot with you. what was my previous guidance?"*); **(3)** four instances of an unverified or one-search-form claim entering an artifact — a false `--collect-only` premise written into an operator-approved requirement, a constant-name grep that found 2 of 3 guards, an `ls` of one directory generalized to "never existed", and a `goal_active` grep in the wrong tree concluding no `/goal` signal exists when `IC_ROLE=worker` does; **(4)** the operator requested Fable review twice and was right both times — a branch audit found ~190 lines of real bloat, and an adversarial design review found the proposed queue-item-2 design was a **no-op**. | **Run the discussion before scoping anything.** Weigh reviewer-side and mechanical triggers over further author-side text, per the established finding that procedure beats directive: candidate tripwires are a confident claim resting on one search form, a menu offered where prior guidance already exists, and a design whose causal surface was never opened. Full evidence and cross-links in memory `project_opus_diligence_roadmap_item`. Not started. |
| 17 | `fable_availability._STATE_PATH` conversion | Deferred from Wave 16's migration step 3 on 2026-07-31, by operator decision. Blast radius: ~39 references across 5 files — 26 `monkeypatch.setattr` call sites (`test_orchestrator_mcp.py` x15, `test_brain_client.py` x5, `test_main_validate.py` x3, `test_fable_availability.py` x2, `conftest.py` x1) plus ~13 direct attribute reads in test bodies such as `fa._STATE_PATH.write_text(...)`. It drives **0** suite failures today and is already isolated by `conftest.py`'s `_isolate_fable_state` autouse fixture, so this is scope/consistency debt, not a live defect. | Migrate `_STATE_PATH` to `paths.fable_state_path()`; the ~26 `monkeypatch.setattr` sites become `monkeypatch.setenv("IRONCLAUDE_FABLE_STATE_PATH", ...)`, which is simpler and propagates to subprocesses. Retiring the now-redundant `_isolate_fable_state` autouse fixture belongs to this loop, not Wave 16. Not started. |
| 19 | Post-advisor amendment and review-receipt recovery | Live failure path on 2026-08-16: a single blind plan review returned `HAS-ISSUES`; the corrected plan was recorded as `advisor-remediated`; later source verification found that correction itself unexecutable. Review evidence was bound to the original provider-root session, so moving to a fresh session would require another prohibited blind review. The plan-artifact reseal operation refused a second same-lineage correction after `advisor-remediated`, while a conflicting active receipt from the superseded lineage blocked Task 1 candidate preparation. The supported PM-on surfaces could neither amend the plan nor unwind the authenticated pre-candidate failure. Recovery therefore required explicit operator authority to disable professional mode and deploy a bounded bootstrap. A first attempted repair then reproduced the deadlock: Task 5 passed 132 focused tests and submitted successfully, but `prepare_review_candidate` rejected it because the submitted task did not re-authorize every historical path already sealed in the cumulative active receipt. No pending candidate existed, the task remained `submitted`, verdict recording was impossible, and the no-candidate unwind did not reopen it; the operator again had to deactivate professional mode. A second repair created receipt 72 and reached task review, but approved R0.4 still required current-task coverage of every historical receipt path while the implementation correctly treated those paths as cumulative authority. Review therefore recorded F and reopened Task 5; the single post-advisor reseal had already been consumed, so immutable artifacts could not be corrected inside PM. Review also proved that repository-plus-`HEAD` unwind binding admits a same-commit branch switch. The operator had to deactivate PM a third time and authorize an exact post-F same-lineage correction. On 2026-08-17, receipt 22 exposed the next deterministic variant: the first post-F reseal succeeded, `create_plan` correctly rejected that correction because it changed a non-reopened task, the valid follow-up correction restored every sealed task and widened only the reopened task, but the still-inactive receipt made every further reseal fail with `Conflicting inactive plan artifact receipt exists`; Claude authentication was healthy and unrelated. The repaired re-entry then exposed one more same-family deadlock: a corrected active plan reached Task 4, authority re-entry dropped the workflow to `idle`, and `submit_tier_up_review(advisor-remediated)` rejected the exact proof needed to authenticate continuation because it accepted only `final_plan_prep|executing`. | Make the PM-on path self-recovering without weakening review authority: permit one server-authenticated post-advisor amendment only in `final_plan_prep` with all tasks pending, one earlier canonical `HAS-ISSUES`, one later `advisor-remediated`, no pending candidate or task grade, and no prior use; preserve lineage, active receipt, index, working bytes, refs, and review history. Permit one additional same-lineage artifact correction only after an exact task-boundary F grade reopens the whole current wave, no pending candidate remains, the current plan hash still has the canonical `HAS-ISSUES` plus `advisor-remediated` chain, and the active artifact receipt is unchanged; enter `final_plan_prep` for corrected reload and make exact replay idempotent. If `create_plan` rejects an inactive correction without activating it, permit the same exact audited recovery to replace that one inactive receipt: authenticate its session/repository/checkout/lineage/audit/ref binding, atomically retire only its row while retaining its ref as evidence, create and audit the corrected inactive receipt, roll back row/ref changes on any failure, and replay idempotently without another blind review. Before candidate creation, permit one transactional retirement of an authenticated stale prior-lineage receipt only when the current submitted task and plan-review evidence match exactly; retain its ref and audit the supersession. Treat authenticated historical receipt paths as cumulative reviewed authority, not as paths the new task must re-authorize; current-task scope gates only new or changed submitted paths, while historical-path drift remains fatal. Every authenticated pre-candidate infrastructure failure must have an exact-binding, idempotent no-candidate unwind bound to repository, checkout, branch, `HEAD`, receipt, lineage, and wave; it changes only the submitted task back to `in_progress`, clears review-scoped flags, preserves active receipt/index/working bytes/refs/review history, records one infrastructure audit and no semantic grade, and makes retry safe. Permit idle `advisor-remediated` only when PM is off and the server verifies the active same-lineage artifact receipt, one active current-wave task, all earlier tasks reviewed, and every changed path inside declared plan scope; then finalization must restore `executing` atomically. All other idle review submissions remain blocked without mutation. Preserve the provider-root session across runtime bootstrap. On any ambiguity, fail closed and request operator guidance with the exact Git commands and effects; approval authorizes only those listed recovery commands. Gate with a full regression replay proving no PM deactivation, second blind review, direct database edit, operator-run Git, commit, or push is needed. **Partially repaired in the current re-entry loop for receipt-22 replacement and authenticated idle-continuation recovery; broader Wave 19 replay remains open.** |
| 20 | Commander execution-complete cleanup authority | Live defect reported 2026-08-16 for Commander directive `d1475`, worker `2d8031c7-1ba3-4722-8de5-cb8a69b70e55`: deliverable commit `d63c9ad35` incidentally tracked a Stage-4 intermediate file. Grader correctly withheld worker termination until the file was untracked, but Brain's orchestrator guard and the worker's `execution_complete` PM guard both blocked `git rm --cached` and the corrective commit. Commander then asked the operator to run both Git commands inside the managed worktree. A stale Commander process started before commit `f07e3e7` also logged `WORKER_DEAD` every poll until the operator restarted it; the new PID emitted the event once. Restart maintenance then produced 22 identity-mismatch errors because `rescueAbandon()` marks an assignment `abandoned` and removes its worktree, while later `cleanupWorkspace()` requires that removed worktree identity to exist; one additional ownerless `active` row has no authenticated release operation. | Add a trusted execution-complete correction lane shared by Commander and direct sessions. It must derive the exact managed worktree, committed parent, reviewed deliverable paths, unwanted tracked path, index state, and proposed commit message from server-held evidence; disclose the exact argv and effects when destructive or ambiguous; consume direct operator guidance when required; run the Git mechanics itself; revalidate the resulting tree and grader condition; then finalize or kill the worker. It must reject path widening, working-byte deletion, whole-index commits, push, cross-worktree use, stale assignment generation, and unreviewed deliverable changes. Add Commander runtime provenance and restart proof, durable dead-event deduplication, a recovery-ref-backed tombstone cleanup for already-removed abandoned worktrees, authenticated ownerless-assignment reconciliation, and bounded aggregate reaper reporting. Acceptance reproduces `d1475`, proves one `WORKER_DEAD` across repeated polls after a verified restart, cleans terminal rows only after proving recovery reachability, preserves ambiguous rows without per-sweep log floods, and proves Commander never asks the operator to run `git rm`, `git commit`, clean, reset, copy, sync, or worktree commands. **Not started.** |
| 21 | Descendant-main workflow and receipt rebinding | Live defect reported from another session on 2026-08-16: reviewed commit `adacc2892` was already an ancestor of current `main`, Task 2 source was unchanged, and repository files, commits, plan documents, and unrelated primary-checkout changes all needed preservation. IronClaude nevertheless treated the existing plan/tasks, `review_pending`, and receipt binding as stale authority with no authenticated continuation path. The assistant recommended `reset_session`; after operator approval, that broad reset returned the workflow to `idle` and discarded durable plan/task/review continuity while leaving professional mode off. The repository survived, but recovery erased more workflow state than the proven descendant-`main` change required and forced another activation/planning cycle. | Add an authenticated descendant-main reconciliation operation and make `reset_session` emergency/admin-only rather than routine recovery guidance. The operation must prove the reviewed commit or receipt is an ancestor of current `HEAD`; bind provider-root session, canonical repository and checkout, branch, current and reviewed parents/trees, lineage, wave, task set, active and pending receipts, index, and working state; prove reviewed/task-scoped source is unchanged or report exact drift; retain refs, grades, plan history, task evidence, and unrelated working/index bytes; then atomically rebind or supersede only stale receipt/plan authority and resume or reopen the exact task. Ambiguous ancestry, branch, path, index, or working-state evidence must fail closed and use AI-assisted operator guidance that lists every proposed Git command and effect before approval; IronClaude executes approved mechanics and never asks the operator to run them. Acceptance reproduces the `adacc2892` descendant-main case, continues without `reset_session`, PM deactivation, another blind review, manual database edits, operator-run Git, commit, push, reset, clean, or stash, and proves rollback leaves all workflow and repository evidence unchanged. **Not started.** |
| 22 | Blind-review quota inheritance and retreat-proof enforcement | Live process violation reported 2026-08-16: lineage 18 received `HAS-ISSUES` and correctly retreated after the advisor classified the defect `REQUIRES-RETREAT`; lineage 19 then received `HAS-ISSUES`, but its advisor explicitly classified the correction `PLAN-ONLY REPAIR`. When a guard blocked the design-document edit, the agent retreated anyway, created lineage 20, and thereby obtained a third blind review. Each numeric lineage individually had one review, but retreat was used as a review-quota reset. The roadmap's current Operating Rule 4 also encodes the wrong behavior by allowing a “fresh reviewer” after plan-only repair. | Replace numeric-lineage counting with one durable blind-review quota per review family, inherited by every retreat/successor lineage. The first terminal blind verdict, including `HAS-ISSUES`, permanently consumes the quota. `PLAN-ONLY REPAIR` forbids review-renewing retreat: apply one coherent advisor remediation, record `advisor-remediated`, and proceed without another blind review. `REQUIRES-RETREAT` may create a successor lineage only when authenticated requirements/design evidence requires it, but the successor inherits the consumed quota. Provide a scoped, authenticated way to amend blocked design/requirements/plan artifacts without manufacturing a new review entitlement. Reject and audit duplicate blind-review dispatch before reviewer invocation. Acceptance reproduces the lineage 18→19→20 path and proves that no retreat, replacement lineage, blocked artifact edit, client restart, or session replacement can authorize a second blind review for the same review family. Update Operating Rule 4 only when this behavior is implemented and verified. **Not started.** |
| 23 | Commander-owned primary-resource access without operator terminal handback | Live Commander defect reported 2026-08-16 for directive `d1475`, worker `gm-core-3`, managed assignment `57887158-7af2-4a53-b5df-8f88ade3beab`: pipeline startup required gitignored source PNGs that existed only in the primary checkout. `use-primary-checkout` correctly required trusted human `UserPromptSubmit`, while Commander correctly could not impersonate the operator by typing the command into the worker. With no sanctioned resource-transfer or delegated checkout operation, Brain instructed the operator to attach to tmux, type `/ironclaude:use-primary-checkout`, and detach. The security boundary was correct; the missing lifecycle capability forced manual worktree management. | Preserve human-only checkout authority while eliminating terminal handback. Add a server-mediated Commander recovery flow that inventories required ignored resources, proves their canonical primary paths and modes, binds repository/session/worker/assignment generation/task allowed paths and intended effects, and presents an operator decision through the normal guidance/elicitation surface. Approval authorizes only the disclosed operation; IronClaude then performs the mechanics itself. Prefer plan-scoped resource adoption or a read-only resource projection into the managed worktree when safe; use a transactional primary-checkout transition only when genuinely required, with manager/guard/Git-authority post-transition parity and rollback on disagreement. Never copy arbitrary ignored files, widen task scope, expose secrets, let Commander forge human intent, or ask the operator to attach to tmux or run Git/worktree commands. Acceptance reproduces `gm-core-3`, starts `config_loader.validate_config()` with the required PNGs available, preserves unrelated primary/index bytes, and completes without operator terminal interaction, PM deactivation, raw Git handback, commit, or push. **Not started.** |
| 24 | Provider-native PM re-entry parity and zero-command recovery | Live Codex defect reported 2026-08-16: native deactivation wrote the legacy `professional_mode_off` audit and changed the session to `off` without invoking trusted authority-epoch closure, so public re-entry inspection found no epoch. A one-shot trusted helper repair then exposed a second shipped-runtime defect: workspace recovery treated ordinary porcelain-v2 kind-1 staged additions/deletions as conflicts because it searched `XY` for `[UDAC]`, even though `git ls-files -u` was empty and every record carried `N...`. It disclosed only `conflict-requires-new-guidance`, opened an unnecessary operator dialogue, and left no executable receipt. Another session also returned a Claude-bound epoch while Codex was active. | Unify Claude/Codex deactivation on the same trusted epoch transaction and bind client identity from the provider-native root, not a default. Define conflicts exclusively from porcelain-v2 unmerged record kind `u` and cross-check the unmerged index in regression fixtures. When all mutable paths are already staged and no unmerged entry exists, create and consume an authenticated zero-command recovery receipt automatically; do not elicit, reconfirm, or bother the operator. Require operator guidance only for a disclosed operation that changes Git state, and list exact argv/effects before selection. Gate source and built bundles independently so a fixed source with stale generated runtime cannot pass release. Acceptance reproduces the missing-epoch, false-conflict, staged-only, client-mismatch, MCP-timeout, and stale-bundle paths in fresh Claude and Codex runtimes. **Not started; current one-shot repair is recovery evidence, not completion.** |
| 25 | **Critical direct-human PM deactivation availability** | Live direct-session failure reported 2026-08-17 for session `d2bc8c6d-db12-4253-99c3-7a2094011d5a`: the operator entered `/ironclaude:deactivate-professional-mode`; trusted `UserPromptSubmit` recognized the human command, but `state-activator` refused the transition with `Trusted professional-mode deactivation could not be finalized: trusted provider root is invalid`. This converts a guardrail-disabling command that only the human may issue into a dead-end whenever installed-root discovery is stale, missing, symlinked, cache-version-skewed, or otherwise rejected. Restarting or asking the operator to repair paths is not an acceptable authority path. | Preserve the established human-only `UserPromptSubmit` boundary while making deactivation independent of disposable cache paths. Durable provider-session bindings, append-only installation observations, immutable stable-runtime generations, monotonic fencing leases, and one primary/emergency state-manager closure preserve mode/epoch/audit atomicity without permitting agent, Commander, subagent, automation, malformed, or missing-session authority. Acceptance reproduces stale/deleted/forged roots, proves workflow and Git preservation, and verifies fresh Claude/Codex installed runtimes. **Implementation and focused source/hook parity are complete in Wave 25; final cachebuster, full suite, installation, fresh-runtime proof, and review remain.** |
| 26 | **Commander startup PM-state and authority-epoch coherence** | Live release failure reproduced 2026-08-17/18 in provider-root session `019fc5e5-fc72-7493-b785-bee8cda62b1b`: after reviewed plugin installation and Codex restart, Commander `_init_brain_session_background` executed a direct SQLite `UPDATE sessions SET professional_mode='off'` and audit `daemon:brain_init/professional_mode_off` at `2026-08-18 04:57:36`. It overwrote an executing session that had a valid active authority epoch `5f18b03a-08ac-435e-a2f6-510f13d808a9` without closing that epoch. Re-entry correctly generated a zero-command receipt for the conflict-free staged checkout, but finalization rejected `Authority re-entry already has a different active epoch`; the operator had to invoke deactivation again solely to create a coherent closed epoch. Restarting caused the defect rather than repairing it. | Remove Commander startup's direct session-mode overwrite. Route initialization through one state-manager-owned transactional operation: create an `off` session only when no session exists; preserve every existing session's professional mode, workflow, plan, tasks, receipts, and authority epochs; never write `off` without a matching authenticated epoch closure. Bind Commander Brain identity without reusing or mutating an unrelated direct provider-root session. Gate with startup/restart matrices for absent and existing `undecided|on|off` sessions, active/closed/missing epochs, executing plans, repeated daemon starts, and concurrent direct sessions. Acceptance proves an `on` executing session remains `on` across Commander and Codex restarts, an intentionally `off` session remains re-enterable, no raw Commander SQL can create mode/epoch disagreement, zero-command activation needs no operator repair, and no restart, manual state edit, Git operation, or redundant deactivation is required. **Run immediately after Wave 25.** |
| 27 | Previously approved authority must not trigger redundant permission prompts | Repeated PM-loop friction: after the operator approved a bounded design, plan, recovery, Git command/effect list, or standing multi-wave objective, agents asked again for permission at the next mechanical transition. This turns explicit authority into a conversational loop and can deadlock unattended execution. | Persist an exact authority receipt binding actor, session, repository, plan/task/recovery identity, scope, commands/effects, expiry, and consumed state. Before eliciting, compare the proposed action against that receipt. Continue silently when it is the same authorized action; ask only when destination, sensitivity, destructive disposition, command/effect set, or scope materially changes. Tests cover brainstorm-to-plan-to-execute transitions, approved recovery command execution, provider-rendered slash/skill forms, replays, scope drift, and concurrent sessions. **Backlog after the current ten-wave objective; do not weaken PM controls or infer broad authority from vague assent.** |
